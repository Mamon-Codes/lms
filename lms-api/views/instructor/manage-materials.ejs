<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Course Materials</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }

        .gradient-bg {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        }
    </style>
</head>

<body class="bg-gray-50">
    <nav class="gradient-bg text-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <h1 class="text-2xl font-bold">👨‍🏫 Instructor Portal</h1>
                <div class="flex items-center space-x-6">
                    <a href="/instructor/dashboard" class="hover:text-gray-200">Dashboard</a>
                    <a href="/logout" class="hover:text-gray-200">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="max-w-6xl mx-auto px-4 py-8">
        <div class="bg-white rounded-2xl shadow-2xl p-8">
            <div class="mb-8">
                <a href="/instructor/dashboard" class="text-pink-600 hover:text-pink-800 mb-4 inline-block">← Back to
                    Dashboard</a>
                <h2 class="text-3xl font-bold mb-2">Manage Course Materials</h2>
                <p class="text-xl text-gray-600">
                    <%= course.title %>
                </p>
            </div>

            <% if (error) { %>
                <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6">
                    <%= error %>
                </div>
                <% } %>

                    <% if (success) { %>
                        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg mb-6">
                            <%= success %>
                        </div>
                        <% } %>

                            <!-- Existing Materials -->
                            <div class="mb-8">
                                <h3 class="text-2xl font-bold mb-4">📚 Existing Materials (<%= materials.length %>)</h3>

                                <% if (materials.length> 0) { %>
                                    <div class="space-y-4">
                                        <% materials.forEach((material, index)=> { %>
                                            <div class="border border-gray-200 rounded-lg p-4 hover:bg-gray-50">
                                                <div class="flex items-start justify-between">
                                                    <div class="flex-1">
                                                        <div class="flex items-center gap-3 mb-2">
                                                            <span
                                                                class="px-2 py-1 bg-purple-100 text-purple-800 rounded text-xs font-semibold uppercase">
                                                                <%= material.type %>
                                                            </span>
                                                            <h4 class="font-bold text-lg">
                                                                <%= material.title %>
                                                            </h4>
                                                        </div>
                                                        <p class="text-gray-600 text-sm mb-2">
                                                            <%= (material.content_text || material.content_url || ''
                                                                ).substring(0, 100) %>
                                                                <%= ((material.content_text || material.content_url
                                                                    || '' ).length> 100) ? '...' : '' %>
                                                        </p>
                                                        <p class="text-xs text-gray-400">Order: <%= material.order_index
                                                                %>
                                                        </p>
                                                    </div>
                                                    <form method="POST"
                                                        action="/instructor/course/<%= course.id %>/material/<%= material.id %>/delete"
                                                        onsubmit="return confirm('Are you sure you want to delete this material?');"
                                                        class="ml-4">
                                                        <button type="submit"
                                                            class="px-3 py-1 bg-red-500 text-white rounded hover:bg-red-600 transition text-sm">
                                                            🗑️ Delete
                                                        </button>
                                                    </form>
                                                </div>
                                            </div>
                                            <% }) %>
                                    </div>
                                    <% } else { %>
                                        <div class="text-center py-12 bg-gray-50 rounded-lg">
                                            <div class="text-6xl mb-4">📝</div>
                                            <p class="text-gray-600">No materials yet. Add your first material below!
                                            </p>
                                        </div>
                                        <% } %>
                            </div>

                            <!-- Add New Material -->
                            <div class="border-t border-gray-200 pt-8">
                                <h3 class="text-2xl font-bold mb-4">➕ Add New Material</h3>

                                <form method="POST" action="/instructor/course/<%= course.id %>/material/add">
                                    <div class="grid md:grid-cols-2 gap-4 mb-4">
                                        <div>
                                            <label class="block text-sm font-semibold mb-2">Material Title*</label>
                                            <input type="text" name="title" required
                                                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                                                placeholder="e.g., Introduction to the Course">
                                        </div>
                                        <div>
                                            <label class="block text-sm font-semibold mb-2">Type*</label>
                                            <select name="type"
                                                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-pink-500 focus:border-transparent">
                                                <option value="text">Text</option>
                                                <option value="video">Video (URL)</option>
                                                <option value="audio">Audio (URL)</option>
                                                <option value="mcq">MCQ/Quiz</option>
                                            </select>
                                        </div>
                                    </div>

                                    <div class="mb-4">
                                        <label class="block text-sm font-semibold mb-2">Material Order*</label>
                                        <input type="number" name="order" required min="1"
                                            value="<%= materials.length + 1 %>"
                                            class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                                            placeholder="Order number">
                                        <p class="text-xs text-gray-500 mt-1">Lower numbers appear first</p>
                                    </div>

                                    <div class="mb-6">
                                        <label class="block text-sm font-semibold mb-2">Content*</label>
                                        <textarea name="content" required rows="6"
                                            class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                                            placeholder="For text: Write your content here&#10;For video/audio: Enter the URL&#10;For MCQ: Enter in JSON format"></textarea>
                                        <p class="text-xs text-gray-500 mt-1">For MCQ, use JSON format: {"question":
                                            "...", "options": [...], "correct": 0}</p>
                                    </div>

                                    <button type="submit"
                                        class="w-full py-3 gradient-bg text-white rounded-lg font-semibold hover:shadow-xl transition">
                                        ➕ Add Material
                                    </button>
                                </form>
                            </div>
        </div>
    </div>
</body>

</html>